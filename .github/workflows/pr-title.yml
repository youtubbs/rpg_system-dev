name: Validate PR Title

on:
  pull_request_target:
    types: [opened, edited, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  validate:
    runs-on: ubuntu-slim
    steps:
      - uses: scarf005/conventional-prs@v0.5.1
